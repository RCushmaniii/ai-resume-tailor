{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter client build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!assets|docs|legal|favicon|mockServiceWorker).*)", "destination": "/index.html" }
  ]
}
