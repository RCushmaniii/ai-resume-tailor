{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter ai-resume-tailor-client build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!assets|favicon|mockServiceWorker).*)", "destination": "/index.html" }
  ]
}
